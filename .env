# For local development, you can use either:
# PostgreSQL (recommended):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gamespill

# Or SQLite for quick testing (if supported):
# DATABASE_URL=file:./dev.db

NODE_ENV=development
PORT=5000

# Optional: Custom deployment URL for meta tags
# VITE_DEPLOYMENT_URL=http://localhost:5000
